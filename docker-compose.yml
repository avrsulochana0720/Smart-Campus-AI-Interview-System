services:
  db:
    image: mysql:8.0
    restart: always
    environment:
      MYSQL_DATABASE: smart_campus_interview
      MYSQL_ALLOW_EMPTY_PASSWORD: "yes"
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data:
